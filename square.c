#include <stdio.h>
int main (void)
{
int length,breadth;
printf ("enter length");
scanf(" %d",&length);
printf("enter breadth");
scanf("%d",&breadth);
printf("answer is %d",length*breadth);


return 0;
}